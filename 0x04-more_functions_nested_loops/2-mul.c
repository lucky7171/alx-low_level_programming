#include "main.h"
/**
 * mul - function that multiplies two integers
 * @a: number that multiplies the other one
 * @b: number to be multiplied
 * Return: the result of the output
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
